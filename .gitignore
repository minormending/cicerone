node_modules/
out/
dist/
*.tsbuildinfo
.DS_Store
.env
# Written by the Supabase CLI: the linked project ref and a version marker.
supabase/.temp/
.probe/
# Rendered books. Never committed: one contains every address the traveller
# sleeps at, their bookings and their notes, and the default filename comes
# from the trip title, so naming them one at a time was always going to miss
# one. No HTML belongs at the root of this repo — the app's own lives in web/.
/*.html
